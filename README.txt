
Para poder generar las clases Java a partir de los contratos
de Solidity usaremos el plugin de Maven 'web3j-maven-plugin'.

Este plugin incorpora una version antigua del compilador de
Solidity, por lo que, para compilar con una versión actuali-
zada, deberemos instalarlo en el sistema.

Para instalar una versión del compilador de Solidity (solc)
basta con descargar una versión del siguiente enlace:

https://github.com/ethereum/solidity/releases

... descomprimir el contenido en algun directorio, y añadir
al PATH la ruta al directorio seleccionado.